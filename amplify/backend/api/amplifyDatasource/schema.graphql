type Task @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  description: String
  taskCategory: TaskCategoryEnum
}

enum TaskCategoryEnum {
    ARRAY_LIST
    LINKED_LIST
    HASHTABLE
    INSERTION_SORT
}
